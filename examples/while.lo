main is (args, system) {

    path = [];
    num = 0;

    max is 10001;

    while num < max {

        if num % 1000 == 0 {
            path = [num] >< path;
        }

        num++;
    }

    system.out.writeln `path`;
};


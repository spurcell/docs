references:

Timer   <exa:timer>


main is -> (io, system) {

    io.out.write("ready\n");

    @Timer:start(1000) -> {
        io.out.write("set\n");
    };

    @Timer:start(2000) -> {
        io.out.write("go!\n");
    };
};
